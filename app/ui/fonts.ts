import { Inter } from 'next/font/google'
import { Lusitana } from 'next/font/google'

export const inter = Inter({ subsets: ['latin'], weight: ['400'] })

export const secondaryFont = Lusitana({ subsets: ['latin'], weight: ['700'] })
